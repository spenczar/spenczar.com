{% extends "base.html" %}

{% block main %}
  <ol class="archives">
    {% for article in dates | reverse %}
    <li>
      <a href="{{ SITEURL }}/{{ article.url }}">{{ article.title }}</a> ({{ article.date.strftime("%B %d, %Y") }})
    </li>
    {% endfor %}
  </ol>
{% endblock main %}
